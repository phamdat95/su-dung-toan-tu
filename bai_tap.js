    var height;
    var width;
    var height = parseInt(prompt("Enter the height"));
    var width =parseInt(prompt("Endter the width"));
    var area = height * width;
    document.write("the area is:" + area);
